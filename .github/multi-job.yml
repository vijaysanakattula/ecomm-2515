name: Multi Job workflow
on:
  - workflow_dispatch  # manual trigger
  - push  # on push it will trigger
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: greet
        run: echo "welcome to multi job"
      - name: welcome
        run: echo "welcome to first-job"  # manual shell commands
    second-job:
      runs-on: ubuntu-latest
      steps:
        - name: greet
          run: echo "welcome to multi job"
          
